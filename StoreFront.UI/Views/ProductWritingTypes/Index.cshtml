@model IEnumerable<StoreFront.DATA.EF.ProductWritingType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Product.ProductName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WritingType.WTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Product.ProductName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WritingType.WTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PWTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PWTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PWTypeID })
        </td>
    </tr>
}

</table>
